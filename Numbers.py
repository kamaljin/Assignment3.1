# Initialize the starting number
num = 1

# Use a while loop to iterate through numbers 1 to 1000
while num <= 1000:
    # Check if the number is divisible by three
    if num % 3 == 0:
        # Print the number if it's divisible by three
        print(num)
    # Move to the next number
    num += 1
